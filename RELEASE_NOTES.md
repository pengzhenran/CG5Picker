CG5Picker —— CG-5 重力仪数据挑选 **v1.0.0**（2026-09-21 修订 1），Windows 10/11（64 位），免费，**完全离线运行**。

**资产**：`CG5Picker_Setup_v1.0.0.exe`（47,128,594 字节，约 44.9 MB）
**SHA256**：`18F75412ED9E378B50F64D0D0E3074FF04EDFFFB962AEB26D1BECBE6F9B14372`

> ### 修订 1（2026-09-21）
>
> 首发包装完之后，在**真实安装**里发现两个问题，已修好并重新出包（**版本号与文件名不变**，
> 网盘链接、README 里的名字都照旧）：
>
> 1. **开始菜单里的「作者信息（关于）」建不出来**，安装时会弹
>    「IPersistFile::Save 失败；错误代码 0x80070003。系统找不到指定的路径。」
>    原因是快捷方式名字写成了 `作者信息 / 关于` —— 那个 `/` 是 Windows 的路径分隔符，
>    文件系统于是去找一个叫「作者信息 」的子目录。现在改成 `作者信息（关于）`。
>    （**静默安装时这个错误会被 SUPPRESSMSGBOXES 吞掉、退出码仍然是 0**，所以只跑静默安装看不出来，
>    当时就是这么漏过去的。）
> 2. 那个快捷方式原来指向 `--version`，而程序是 GUI 子系统、没有控制台 ——
>    双击它**屏幕上什么都不会出现**。现在指向新的 `--about`，直接打开「关于」窗口；
>    `--version` / `--licenses` 在没有控制台时也会自动开窗口，不再"打印了没人看得见"。
>
> 顺带修掉卸载残留：程序运行期自己建的 `config\` 现在随卸载一起删，
> 卸载后安装目录不会只剩一个空壳。
>
> 如果你手上是**首发那个包**（SHA256 以 `BA61B02D` 开头），换成本页现在这个。

### 这个工具做什么

CG-5 重力仪一次导出的手簿常含**多天、多个 Survey**。CG5Picker 把它挑成一份能直接用的表：

**按日期 / Survey 两条线圈范围 → 按条件筛选 → 图形上人工复核 → 按原始数据格式导出 xlsx。**

### 功能

- **两条线定范围**：按日期 或 按 Survey，另一边自动跟随；两边**取交集**，缺一边就是 0 条
- **条件栈**：观测时长 / 相邻互差 / 分段间隔 / 最终取值（**最新** 或 **最小距平**），可 **且 / 或** 分组，参数组合可存可换
- **分段间隔**单独一条条件：同一测点两次观测间隔超过阈值就分段
- **逐轮挑选**：同一测点按时间分段，**每轮单独给「最终取值」**（同一点来回观测也能挑对）
- **互差**：按测点在时间上滑动比较，超限的观测标出来，互差曲线单独一个视图
- **图形复核**：测点分布 / 观测值 / 仪器潮汐 / 理论固体潮 / 互差，滚轮缩放、拖拽平移、框选、悬停读数
- **人工复核留痕**：手动勾选或剔除**逐条记理由**，可以「只看人工改过的」，也可以一键恢复条件结果
- **导出 xlsx**：列序可调；**日期时间是真正的日期时间**（WPS / Excel 直接认）、编号保留原文小数位、可附 Survey 文件头
- **不污染系统**：配置写在**安装目录** `config\`（不是 C 盘用户目录）；全程离线；默认最大化启动
- 界面里 **F1 = 使用说明**（HTML，窗口内预览），**Shift+F1 = 关于**

### 安装

1. 下载本页的 `CG5Picker_Setup_v1.0.0.exe`，双击按向导安装（**中文向导**）。默认**按用户安装**
   （不弹管理员）到 `%LOCALAPPDATA%\Programs\CG5Picker`；向导里也能改到别的盘
2. 开始菜单生成 `CG5 数据挑选` / `使用说明 (HTML)` / `作者信息（关于）` / `许可与第三方声明` / `卸载`，
   可选桌面快捷方式
3. **不需要装 Python**：Python 运行时、Qt、绘图库都在安装目录里
4. 验证安装（退出码 0 即正常）：

```cmd
"%LOCALAPPDATA%\Programs\CG5Picker\CG5Picker.exe" --version
```

`CG5Picker.exe --selftest <CG5手簿.txt>` 还会跑一遍无界面自检（解析 → 条件 → 列序 → 导出）。

### 首次运行可能被 Windows 拦下

程序与安装包都**没有代码签名**：

- 「Windows 已保护你的电脑 / 未知发布者」→ 点「更多信息」→「仍要运行」
- 「智能应用控制已阻止可能不安全的应用」→ 这是 Windows 11 的智能应用控制，没有单应用白名单，
  只能关闭：设置 → 隐私和安全性 → Windows 安全中心 → 应用和浏览器控制 → 智能应用控制 → 关闭，
  然后重新双击程序（不必重装）

换安装目录、卸载重装都不能绕过第二种提示 —— 它只看数字签名，不看路径。关闭智能应用控制不影响
Defender 的病毒防护。

### 已知限制

- 手簿里**格式坏掉的行**按"如实"处理，会作为一条观测出现；它不影响其它行的判定
- 理论固体潮需要 Survey 头里有经纬度，缺坐标时图上会注明，该视图没有高亮点
- `line` 号在很多手簿里恒为 `0`，"按测点分组"实际主要由**点号**决定
- 观测时长阈值（默认 ≥35 s）按天可能不同：调大了有可能把**某天全部观测剔光**，
  这不是软件出错，汇总框会点名这条条件
- 实测手簿属于野外数据，**不随本仓库与本包分发**；把任意一份 CG-5 手簿拖进界面即可试用

### 许可与出处

本软件代码 **MIT**（`LICENSE.txt`）。随包第三方组件全部是宽松或弱 copyleft 许可：
**PySide6 / Qt → LGPLv3**（动态链接、未修改、可替换）、**matplotlib → PSF/BSD 风格**、
**NumPy → BSD-3**、**openpyxl → MIT**、Python 运行时 → PSF。**不使用**任何 GPL-only 的 Qt 模块
（Qt Charts / Data Visualization / Graphs），绘图一律 matplotlib。
逐条声明与全文见包内 `licenses\`，界面「关于 → 许可与第三方声明」也能看。

Copyright (c) 2026 彭桢燃（Zhenran Peng），中国地质大学（武汉）　<zhenran.peng@cug.edu.cn>

夸克网盘（国内下载更快）：<https://pan.quark.cn/s/615510f3f836>　·　
课题组公众号「地球重力与人类生活（TVGG）」二维码见仓库 README 文末。
